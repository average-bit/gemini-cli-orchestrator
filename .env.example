# Optional: Specify custom path to Gemini CLI if not in system PATH
# GEMINI_CLI_PATH=/path/to/your/gemini

# The tool will use 'gemini' from your system PATH by default
# Only set GEMINI_CLI_PATH if you have a custom installation location